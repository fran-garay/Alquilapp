<!DOCTYPE html>
<html>
  <head>
    <title>Alquilapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
  </head>

  <body>

    <% if notice != nil%>
       <div class="alert alert-success alert-dismissible fade show p-2" role="alert" style="margin-bottom:1px">
          <%= notice %>
          <button type="button" class="btn-close p-2" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <%end%>
    <% if alert != nil%>
      <div class="alert alert-danger alert-dismissible fade show p-2 d-flex" role="alert">
          <%= alert %>
          <button type="button" class="btn-close p-2" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <%end%>
    <div class="d-flex flex-grow border border-success border-5">
      <div class="d-flex border flex-grow border-danger">
        <% if admin_signed_in? %>
          <%= render "application/menu_admins" %>
        <% elsif user_signed_in? %>
          <%= render "application/menu_users" %>
        <% end %>
        <div class="container" style="margin-top:3vh">
        <%= yield %>
        </div>
      </div>
    </div>
  </body>

  <script>
    // delete myalert after 5 seconds
    setTimeout(function() {
      var element = document.getElementsByClassName("alert");
      element[0].parentNode.removeChild(element[0]);
    }, 3000);

    // hide and show navbar when button b is pressed
    var b = document.getElementById("b");
    var navbar = document.getElementById("nav");
    b.addEventListener("click", function() {
      if (navbar.style.display === "none") {
        navbar.style.display = "block";
      } else {
        navbar.style.display = "none";
      }
    });

  </script>
</html>
